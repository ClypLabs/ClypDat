<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="ClypDat.App.App">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceInclude Source="avares://ClypDat/Styles/Tokens.axaml" />
      </ResourceDictionary.MergedDictionaries>
      <!-- Fallback colors; overwritten at startup in App.axaml.cs with the live
           Windows accent color when available (Settings > Personalization > Colors). -->
      <SolidColorBrush x:Key="AccentBrush" Color="#5864E8" />
      <SolidColorBrush x:Key="AccentBrushHover" Color="#6D77F0" />
      <!-- Sidebar selection tints, derived from the accent above (see
           ApplyAccentColor) so they track the Windows colour too. -->
      <SolidColorBrush x:Key="AccentSelectedBrush" Color="#2A3350" />
      <SolidColorBrush x:Key="AccentSelectedHoverBrush" Color="#222A43" />
      <SolidColorBrush x:Key="AccentSelectedIconBrush" Color="#A8B4F5" />
      <SolidColorBrush x:Key="AccentGameHoverBrush" Color="#222A43" />
      <!-- Folder tiles use the former brighter rail tint while idle. -->
      <SolidColorBrush x:Key="AccentFolderBrush" Color="#2C3568" />
      <!-- Hovering a rail button uses a darker version of its selection tint,
           keeping selected state visually stronger than pointer-over state. -->
      <SolidColorBrush x:Key="AccentHoverBrush" Color="#222A43" />
      <!-- These duplicate merged token defaults intentionally. Theme
           selection mutates application-local brushes in place. -->
      <SolidColorBrush x:Key="AppBgBrush" Color="#0D1116" />
      <SolidColorBrush x:Key="PanelBgBrush" Color="#101820" />
      <SolidColorBrush x:Key="SurfaceBrush" Color="#141D24" />
      <SolidColorBrush x:Key="SurfaceRaisedBrush" Color="#1A242E" />
      <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#22303D" />
      <SolidColorBrush x:Key="EdgeBrush" Color="#232F3A" />
      <SolidColorBrush x:Key="EdgeStrongBrush" Color="#2C3B48" />
      <SolidColorBrush x:Key="DividerBrush" Color="#1B242D" />
    </ResourceDictionary>
  </Application.Resources>
  <Application.Styles>
    <FluentTheme />
    <StyleInclude Source="avares://ClypDat/Styles/AppStyles.axaml" />
  </Application.Styles>
</Application>
