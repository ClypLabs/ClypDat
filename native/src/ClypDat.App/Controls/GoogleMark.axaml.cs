using Avalonia.Controls;

namespace ClypDat.App.Controls;

/// <summary>
/// The Google "G", in the four brand colours it is required to keep. Sized by
/// the caller: everything inside is vector inside a Viewbox.
/// </summary>
public sealed partial class GoogleMark : UserControl
{
    public GoogleMark() => InitializeComponent();
}
