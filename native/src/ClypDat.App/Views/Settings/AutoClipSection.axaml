<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ClypDat.App.ViewModels"
             xmlns:controls="using:ClypDat.App.Controls"
             xmlns:conv="using:Avalonia.Data.Converters"
             xmlns:opconv="using:ClypDat.App.Converters"
             x:Class="ClypDat.App.Views.Settings.AutoClipSection"
             x:DataType="vm:MainWindowViewModel">
  <UserControl.IsVisible>
    <MultiBinding Converter="{x:Static opconv:SettingsSectionVisibleConverter.Instance}" ConverterParameter="Auto-Clip">
      <Binding Path="SettingsSearchText" />
      <Binding Path="SelectedSettingsSection" />
    </MultiBinding>
  </UserControl.IsVisible>
  <StackPanel Spacing="24" Tag="Auto-Clip">
    <Border Classes="settingsDivider" Margin="0,0,0,4" IsVisible="{Binding IsSettingsSearchActive}" />
    <StackPanel Spacing="6">
      <TextBlock Classes="settingsSectionTitle" Text="Auto-clipping" />
      <TextBlock Classes="settingsSectionDesc" Text="Have ClypDat save important moments using local event feeds or captured game frames." />
      <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8" Margin="0,4,0,0" MaxWidth="640" HorizontalAlignment="Left">
        <PathIcon Classes="cautionIcon" VerticalAlignment="Top" Margin="0,2,0,0" />
        <TextBlock Grid.Column="1" Classes="settingsRowWarning"
                   Text="Experimental: still being worked on, and can miss events, mislabel clips or misbehave." />
      </Grid>
    </StackPanel>
    <!-- DataContext switches to the PolicyPause, so visibility lives on the wrapper. -->
    <Panel IsVisible="{Binding AutoClippingPolicyBlocked}">
      <controls:PolicyPauseBanner DataContext="{Binding AutoClippingPolicyPause}" />
    </Panel>

    <StackPanel Classes="settingsGroup" IsVisible="{Binding SettingsSearchText, Converter={x:Static opconv:SettingsSearchHighlightConverter.Instance}, ConverterParameter=Auto Capture Events}">
      <TextBlock Classes="settingsGroupLabel" Text="AUTO CAPTURE" opconv:SettingsHighlight.Query="{Binding SettingsSearchText}" />
      <Border Classes="settingsGroupCard">
        <Border Classes="settingsRow">
          <Grid Classes="settingsRowGrid" ColumnDefinitions="*,Auto">
            <StackPanel Classes="settingsRowText">
              <TextBlock Classes="settingsRowTitle" Text="Auto capture events" opconv:SettingsHighlight.Query="{Binding SettingsSearchText}" />
              <TextBlock Classes="settingsRowDesc" Text="Turn this off to pause every game without losing your event choices." opconv:SettingsHighlight.Query="{Binding SettingsSearchText}" />
            </StackPanel>
            <ToggleSwitch Grid.Column="1" IsChecked="{Binding AutoClippingEnabled}" IsEnabled="{Binding !AutoClippingPolicyBlocked}" OnContent="{x:Null}" OffContent="{x:Null}" />
          </Grid>
        </Border>
      </Border>
    </StackPanel>

    <StackPanel Classes="settingsGroup">
      <TextBlock Classes="settingsGroupLabel" Text="GAMES" />
      <TextBox Classes="settingsInput" Text="{Binding AutoClipSearchText}" PlaceholderText="Search auto-clipping games or events" Margin="0,0,0,2" />
      <ItemsControl ItemsSource="{Binding AutoClipGames}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Classes="settingsGroupCard" Padding="10" Margin="0,0,0,10" IsVisible="{Binding IsSearchMatch}">
              <StackPanel Spacing="0">
                <!-- The expand button spans every column and sits FIRST, so it
                     renders underneath: later children take the hit, which lets
                     the switch keep its own bounds while a click anywhere else
                     on the row opens the game. Nothing inert is hit-testable, or
                     it would swallow clicks meant for the row. -->
                <Grid ColumnDefinitions="22,Auto,*,Auto,Auto" ColumnSpacing="12" MinHeight="72">
                  <Button Grid.ColumnSpan="5" Classes="autoClipRow" Click="AutoClipGameExpandButton_OnClick" />
                  <PathIcon Grid.Column="0" Classes="autoClipChevron" IsHitTestVisible="False"
                            IsVisible="{Binding !IsExpanded}" Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z" />
                  <PathIcon Grid.Column="0" Classes="autoClipChevron" IsHitTestVisible="False"
                            IsVisible="{Binding IsExpanded}" Data="M7.41,8.58L12,13.17L16.59,8.58L18,10L12,16L6,10L7.41,8.58Z" />
                  <Border Grid.Column="1" Classes="autoClipCover" IsHitTestVisible="False">
                    <Image Source="{Binding CoverImage}" Stretch="UniformToFill" />
                  </Border>
                  <StackPanel Grid.Column="2" Spacing="3" VerticalAlignment="Center" IsHitTestVisible="False">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                      <TextBlock Classes="settingsRowTitle" Text="{Binding Name}" FontSize="15" VerticalAlignment="Center"
                                 opconv:SettingsHighlight.Query="{Binding SearchQuery}" />
                      <!-- This panel ignores hit tests, so the reason is the
                           toggle's tooltip (column 4) rather than the pill's. -->
                      <Border Classes="statusPill caution" IsVisible="{Binding IsDetectorPolicyBlocked}" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                          <Ellipse />
                          <TextBlock Text="Paused by ClypDat" />
                        </StackPanel>
                      </Border>
                    </StackPanel>
                    <!-- Collapsed, this line is all the user gets, so it says
                         how many events are on rather than repeating the name. -->
                    <TextBlock Classes="settingsRowDescSm" Text="{Binding StatusText}" IsVisible="{Binding IsExpanded}" />
                  </StackPanel>
                  <TextBlock Grid.Column="3" Classes="autoClipCount" Text="{Binding SummaryLabel}" IsHitTestVisible="False" />
                  <ToggleSwitch Grid.Column="4" IsChecked="{Binding IsEnabled}" IsEnabled="{Binding !IsPolicyBlocked}" OnContent="{x:Null}" OffContent="{x:Null}" VerticalAlignment="Center"
                                ToolTip.Tip="{Binding DetectorPolicyTip}" ToolTip.ShowOnDisabled="True" />
                </Grid>

                <StackPanel Spacing="0" Margin="34,0,10,6" IsVisible="{Binding IsExpanded}">
                  <!-- Outside the disabled block below: setup is what you do
                       BEFORE the game can be switched on. -->
                  <Button Content="Set up Dota 2" Click="SetupDotaAutoClipButton_OnClick" IsVisible="{Binding IsSetupRequired}"
                          HorizontalAlignment="Left" Margin="0,4,0,0" />

                  <!-- Greys out with the switch above it, so what that switch
                       governs is not left to guesswork. -->
                  <StackPanel Spacing="0" IsEnabled="{Binding IsEnabled}">
                    <StackPanel Spacing="14" IsVisible="{Binding IsCs2}">
                      <Border Classes="settingsDivider" Margin="0,12,0,0" />
                      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="24">
                        <StackPanel Classes="settingsRowText">
                          <TextBlock Classes="settingsRowTitle" Text="Deathmatch clipping" />
                          <TextBlock Classes="settingsRowDesc" Text="CS2 auto-clips save in Competitive matches. Turn this on to also save Deathmatch clips." />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsChecked="{Binding DeathmatchClipping}" OnContent="{x:Null}" OffContent="{x:Null}" VerticalAlignment="Center" />
                      </Grid>
                    </StackPanel>

                    <Border Classes="settingsDivider" Margin="0,12,0,10" />

                    <ItemsControl ItemsSource="{Binding Groups}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <StackPanel>
                            <!-- A one-event group still carries its catalog group ID for
                                 capture scheduling, but it has nothing to expand in the
                                 settings UI. Show its event directly. -->
                            <CheckBox Classes="autoClipEvent" Margin="22,0,0,0"
                                      IsVisible="{Binding IsSingleEvent}"
                                      Content="{Binding SingleEvent.Name}"
                                      IsChecked="{Binding SingleEvent.IsEnabled}" />
                            <StackPanel Spacing="2" Margin="0,0,0,6" IsVisible="{Binding !IsSingleEvent}">
                            <!-- Same two-target arrangement as the game row: the
                                 tri-state box turns the whole group on or off,
                                 the rest of the row expands it. -->
                            <Grid ColumnDefinitions="22,20,*,Auto" ColumnSpacing="8" MinHeight="30">
                              <Button Grid.ColumnSpan="4" Classes="autoClipRow" Click="AutoClipGroupExpandButton_OnClick" />
                              <PathIcon Grid.Column="0" Classes="autoClipChevron" IsHitTestVisible="False"
                                        IsVisible="{Binding !IsExpanded}" Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z" />
                              <PathIcon Grid.Column="0" Classes="autoClipChevron" IsHitTestVisible="False"
                                        IsVisible="{Binding IsExpanded}" Data="M7.41,8.58L12,13.17L16.59,8.58L18,10L12,16L6,10L7.41,8.58Z" />
                              <Button Grid.Column="1" Classes="autoClipGroupToggle" Click="AutoClipGroupToggleButton_OnClick" VerticalAlignment="Center">
                                <Grid Width="20" Height="20">
                                  <Border BorderBrush="{StaticResource TextMutedBrush}" BorderThickness="1" CornerRadius="4" Background="Transparent" />
                                  <Border IsVisible="{Binding IsAllEnabled}" BorderBrush="{DynamicResource CheckBoxCheckBackgroundFillChecked}" BorderThickness="1" CornerRadius="4" Background="{DynamicResource CheckBoxCheckBackgroundFillChecked}" />
                                  <Border IsVisible="{Binding IsIndeterminate}" BorderBrush="{DynamicResource CheckBoxCheckBackgroundFillChecked}" BorderThickness="1" CornerRadius="4" Background="{DynamicResource CheckBoxCheckBackgroundFillChecked}" />
                                  <PathIcon IsVisible="{Binding IsAllEnabled}" Data="M9,20.42L2.79,14.21L5.62,11.38L9,14.77L18.88,4.88L21.71,7.71L9,20.42Z" Width="11" Height="11"
                                            Foreground="{DynamicResource CheckBoxCheckGlyphForegroundChecked}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                  <Border IsVisible="{Binding IsIndeterminate}" Width="10" Height="2" CornerRadius="1"
                                          Background="{DynamicResource CheckBoxCheckGlyphForegroundChecked}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Grid>
                              </Button>
                              <TextBlock Grid.Column="2" Text="{Binding Name}" Foreground="{StaticResource TextStrongBrush}" FontWeight="SemiBold"
                                         VerticalAlignment="Center" IsHitTestVisible="False" />
                              <TextBlock Grid.Column="3" Classes="autoClipCount" Text="{Binding SummaryLabel}" IsHitTestVisible="False" />
                            </Grid>
                            <ItemsControl ItemsSource="{Binding Events}" Margin="42,2,0,4" IsVisible="{Binding IsExpanded}">
                              <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                  <CheckBox Classes="autoClipEvent" Content="{Binding Name}" IsChecked="{Binding IsEnabled}" />
                                </DataTemplate>
                              </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            </StackPanel>
                          </StackPanel>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding UngroupedEvents}" Margin="22,0,0,0">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <StackPanel>
                            <CheckBox Classes="autoClipEvent" Content="{Binding Name}" IsChecked="{Binding IsEnabled}" />
                            <TextBlock Classes="settingsRowDescSm" Text="{Binding Description}" IsVisible="{Binding HasDescription}"
                                       Margin="28,0,0,8" />
                          </StackPanel>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </StackPanel>
                </StackPanel>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </StackPanel>
</UserControl>
